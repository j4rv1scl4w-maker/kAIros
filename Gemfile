source "https://rubygems.org"

# Sito statico generato con Jekyll e pubblicato via GitHub Actions → GitHub Pages.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-sitemap", "~> 1.4"   # sitemap.xml per i motori di ricerca
  gem "jekyll-seo-tag", "~> 2.8"   # Meta tag SEO / Open Graph / Twitter Card
end

# Necessario su Ruby 3+ per il server locale `jekyll serve`
gem "webrick", "~> 1.8"
